# Explorer — Implementation Plan

Spec: `docs/superpowers/specs/2026-07-04-explorer-design.md`
Location: `explorer/` subdirectory (Next.js 14, port 8400)

Follows the milestone order from spec Section 11 (M1–M5). Each milestone is a
Task below: implement via TDD, run `npm test`, `npm run lint`, `tsc --noEmit`,
self-review, commit, then move to the next. No human-review gate.

## Status

- [x] Task 1 / M1: Scaffold + DB + Health — `package.json`, configs, migrations, `lib/db.ts`, `lib/auth.ts`, `lib/adp.ts`, `app/api/health/route.ts`, `app/api/users/register/route.ts`.
- [x] Task 2 / M2: Metrics + Session list API — `lib/metrics.ts`, `lib/sessions.ts`, `app/api/metrics/route.ts`, `app/api/sessions/route.ts`, `app/api/sessions/[id]/route.ts`. All ADP calls mocked via vi.fn() on global.fetch. 38/38 tests passing.
- [x] Task 3 / M3: Search + Top questions API — `lib/search.ts`, `lib/top-questions.ts`, `app/api/search/route.ts`, `app/api/top-questions/route.ts`. Fan-out across known users. Top questions normalized and ranked.
- [x] Task 4 / M4: Web UI — All pages (`/`, `/sessions`, `/sessions/[id]`, `/search`, `/top-questions`) and shared components (`MetricCard`, `SparklineChart`, `SessionTable`, `TraceMessage`, `ConfidenceBadge`, `GuardrailBadge`). Confidence color-coded (green ≥0.8, amber 0.5–0.79, red <0.5). Guardrail badge inline on assistant turns.
- [x] Task 5 / M5: Integration test — Full lifecycle in `tests/integration.test.ts`. 38/38 tests passing, lint clean, typecheck clean.

## Resume instructions if interrupted

1. Check this file's Status section for the last completed task.
2. `cd explorer && npm test` to confirm tests still pass.
3. Continue at the first unchecked task above.
