# Trust & Reliability UI Rebuild — Implementation Plan

Spec: `docs/superpowers/specs/2026-07-04-trust-ui-design.md`
Location: `trust/ui/` (existing Next.js app, port 8501)
Commit prefix: `[trust-ui]`

**Claimed 2026-07-04 — last unclaimed item in the UI Rebuild Initiative (all
other 7 sub-projects done or in progress). No collision: nobody else has
touched `trust/ui/` since Design System's M2 added the `design-system`
dependency to its `package.json`.**

## Grounding against the real (not spec-imagined) API/lib surface

- `trust/ui/lib/api.ts` **already exists and is correct** (verified against
  `trust/src/trust/routes/{system,audit,check}.py`): `getStats()`,
  `listAudit({cursor?, limit?})`, `getAuditRecord(id)`, `getRateLimit(channelId)`,
  `listKnownChannelIds()`. This file needs **no changes** — only the pages and
  styling get rebuilt, per the spec's non-goal of no backend/route changes.
- `design-system` exports: same verified API as the other rebuilds —
  `AppShell`, `Badge` (replaces the local `StatusBadge`), `Table`, `MetricCard`,
  `Card`.
- Trust's own `tailwind.config.ts` and `app/globals.css` currently define an
  unrelated dark-purple theme (`base`, `surface`, `accent`, `pass/warn/block`)
  predating Design System — these get replaced with the shared preset.

## Status

- [ ] M1: Delete old UI pages + `components/StatusBadge.tsx`; rebuild
      tailwind/globals/layout on design-system
- [ ] M2: Dashboard (stats) + rate-limits page
- [ ] M3: Audit log list/detail
