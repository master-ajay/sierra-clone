# Channels — Implementation Plan

Spec: `docs/superpowers/specs/2026-07-04-channels-design.md`
Location: `channels/` subdirectory (separate Python project, own `.venv`, own `pyproject.toml`)
Port: 8200
Commit prefix: `[channels]`

## Status

- [ ] Task 0: Scaffold
- [ ] Task 1 / M1: Scaffold + DB + Health
- [ ] Task 2 / M2: Channel CRUD
- [ ] Task 3 / M3: Chat endpoint (mocked upstreams)
- [ ] Task 4 / M4: Channel stats
- [ ] Task 5 / M5: Widget JS + embed snippet
- [ ] Task 6 / M6: Integration tests

---

### Task 0: Scaffold

```bash
mkdir -p channels/src/channels/{models,routes,services}
mkdir -p channels/{tests,data,migrations}
touch channels/src/channels/__init__.py
touch channels/src/channels/models/__init__.py
touch channels/src/channels/services/__init__.py
touch channels/src/channels/routes/__init__.py
touch channels/tests/__init__.py
touch channels/data/.gitkeep
```

**pyproject.toml**, **requirements.txt**, **.env.example**, **.gitignore**, venv created and deps installed.

---

### Task 1 / M1: Scaffold + DB + Health

Files: `config.py`, `database.py`, `auth.py`, `errors.py`, `main.py`,
`migrations/001_initial_schema.sql`, `routes/system.py`, `tests/conftest.py`,
`tests/test_system.py`.

Done when: `GET /v1/health` → 200, auth tests pass, pytest + ruff clean.

---

### Task 2 / M2: Channel CRUD

Files: `models/channel.py`, `services/channel_service.py`, `routes/channels.py`,
`tests/test_channels.py`.

Done when: create, list, get, update, delete all pass. channel_key is 64 hex chars.
Duplicate channel_key impossible. pytest clean.

---

### Task 3 / M3: Chat endpoint

Files: `models/chat.py`, `services/chat_service.py`, `routes/chat.py`,
`tests/test_chat.py`.

Upstreams (ADP + Agent Runtime) mocked via `respx`.

Done when: POST /v1/channels/{id}/chat returns reply + session_id + citations + trace.
Paused/revoked → 503. Wrong channel_key → 401. Upstream fail → 502. pytest clean.

---

### Task 4 / M4: Channel stats

Update `channel_service.py` + `chat_service.py`. `routes/channels.py` stats endpoint.
`tests/test_stats.py`.

Done when: stats reflect actual message/session counts. pytest clean.

---

### Task 5 / M5: Widget JS + snippet

Files: `services/widget_service.py`, `routes/widget.py`, `tests/test_widget.py`.

Done when: GET /v1/channels/{id}/widget.js returns JS with correct Content-Type.
GET /v1/channels/{id}/snippet returns embed HTML. pytest clean.

---

### Task 6 / M6: Integration test

File: `tests/test_integration.py`.

Done when: create channel → chat → stats → pause → 503 → revoke → 404.
pytest + ruff clean. Commit.
